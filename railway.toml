[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && /app/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[deploy.env]
PYTHON_VERSION = "3.11"