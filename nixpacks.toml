[phases.setup]
nixPkgs = ["python311", "nodejs_18"]

[phases.install]
cmds = ["python3 -m venv /app/.venv", "/app/.venv/bin/pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && /app/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
